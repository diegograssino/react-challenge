import Body from "../Body";

const Loader = () => {
  return (
    <div className="loader">
      <Body as="p" variant="primary">
        Loading ...
      </Body>
    </div>
  );
};

export default Loader;
